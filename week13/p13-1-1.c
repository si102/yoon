#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
/*[실습1-1] 구조체 정의 및 활용-구조체 초기화 까지 
struct student{ //구조체 형태의 변수 선언 
	int ID;
	char name[10];
	double grade;
};

void print_s1(struct student s1){
	printf("ID: %i\nname: %s\ngrade: %f", s1.ID, s1.name, s1.grade);
} 

int main(void){
	struct student s1 ={24, "Siyoon", 4.3}; //구조체 초기값 설정
	print_s1(s1);
	
	return 0; 
}*/
